import 'package:flutter/material.dart';

class FormTransactionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        "Form Transactio",
        textScaleFactor: 2.0,
      ),
    );
  }
}
