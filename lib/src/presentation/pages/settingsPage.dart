import 'package:flutter/material.dart';

class SettingsTransaction extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        "Settings",
        textScaleFactor: 2.0,
      ),
    );
  }
}
