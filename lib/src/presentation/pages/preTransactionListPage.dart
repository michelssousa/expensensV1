import 'package:flutter/material.dart';

class PreTransactionListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        "Pre Transaction List",
        textScaleFactor: 2.0,
      ),
    );
  }
}
